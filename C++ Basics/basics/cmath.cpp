#include <iostream>
#include <cmath>
using namespace std;

int main(){
	
    cout << pow(2 ,3);
    cout << sqrt(49);
    cout << round(2.3); // rounds based on .5 above below
    cout << floor(3.7); // gives lower number doest give based in .5
    cout << ceil(2.3);// upprer  number
    cout << fmax(1,10);
     cout << fmin(1,10); 
    
    
    
	return 0;
}
